Program to detect if text is AI or human generated. 

Instructions : 

Run "pip install -r requirements.txt" to install dependencies. 
Run the checkeractual file 

Dependencies : 
- Python (v>=3.10) 
- Selenium 
- Chrome/Chromedriver
